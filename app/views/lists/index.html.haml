%h1 Twoje listy
%br

%table.table.table-hover
  %tr
    %th Name
    %th
    %th
    %th
    %th

    

  - current_user.lists.each do |list|
    %tr
      %td= list.name      
      %td= button_to 'Show', list, :class => "btn btn-primary", :method => :get
      %td= button_to 'Edit', edit_list_path(list), :class => "btn btn-primary", :method => :get
      %td= button_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-primary"
      

%br
-if user_signed_in?
  = link_to 'New List', new_list_path
%br
